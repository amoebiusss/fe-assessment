// import { StarlinkInfo } from '../../../shared/interfaces';

export function categorizeStarlinks(
    // list: StarlinkInfo[]
): void {
    // this.starlinks.forEach((starlink) => {
    //   // Round the height_km to the nearest integer
    //   const roundedHeight = Math.round(starlink.height_km);

    //   // Categorize based on divisibility rules
    //   if (roundedHeight % 3 === 0 && roundedHeight % 5 === 0) {
    //     this.list3.push(starlink); // Divisible by both 3 and 5
    //   } else if (roundedHeight % 3 === 0) {
    //     this.list1.push(starlink); // Divisible by 3
    //   } else if (roundedHeight % 5 === 0) {
    //     this.list2.push(starlink); // Divisible by 5
    //   } else {
    //     this.list4.push(starlink); // Not divisible by 3 or 5
    //   }
    // });
}